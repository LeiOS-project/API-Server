---
description: Run the TypeScript typecheck
agent: build
---
Run `bun run typecheck` for this repository and report any failures clearly.
If it fails, identify the smallest code change needed to fix it.
